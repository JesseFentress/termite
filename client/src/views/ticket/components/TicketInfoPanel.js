/*import { Panel } from "../../../components/Panel";

export const TicketInfoPanel = ({
    ticket
}) => {
    return (
        <div className="col-4 center">
            <Panel title="Ticket Info" panelSize="panel-tall">
                <div className="col-6">
                    <label
                    htmlFor="title"
                    >
                    Title:</label>
                    <p id="title">{ticket ? ticket.title : null}</p>
                </div>
                <div className="col-6">
                    <label
                        htmlFor="deadline"
                    >Deadline:</label>
                    <p id="deadline">{ticket ? ticket.deadline : null}</p>
                </div>
            </Panel>
        </div> 
    );
};*/
